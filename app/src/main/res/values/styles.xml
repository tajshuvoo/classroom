<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize the theme as needed -->
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <!-- Add other theme attributes if necessary -->
    </style>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize other style attributes if needed -->
    </style>

    <style name="AppTheme.SecondActivity" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize other style attributes if needed -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Remove the windowActionBar attribute -->
        <item name="windowNoTitle">true</item>
        <!-- Other theme attributes -->
    </style>

</resources>